class OnboardingModel {
  final String image, name, title, description;

  OnboardingModel({
    required this.image,
    required this.name,
    required this.title,

    required this.description,
  });
}
